export default function HeroSection() {
    return (
        <section id="home" 
        style={{ backgroundImage: "linear-gradient(rgba(0,0,0,0.8), rgba(0,0,0,0.6)),url('/postprime-hero.png')" }}
        className="h-screen bg-cover bg-center">
            
        </section>
    )
}